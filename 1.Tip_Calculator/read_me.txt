Let's break it.

document

document means:

"My webpage."

JavaScript can use document to access HTML elements.

getElementById()

This means:

"Find something by its ID."

toFixed(2)

means:

"Show exactly 2 numbers after the decimal."

btnClick.addEventListener("click", calculateTotal);

This means:

"When the user clicks the button, run calculateTotal()."